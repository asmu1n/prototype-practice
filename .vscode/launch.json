{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bun",
            "request": "launch",
            "name": "Debug Current Bun File",
            "program": "${file}",
            "cwd": "${workspaceFolder}",
            "runtime": "bun"
        }
    ]
}
